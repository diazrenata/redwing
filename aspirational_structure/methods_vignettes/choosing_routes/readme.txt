Use `drake_caches/metadata_cache.sqlite` for the cache here.

Then work through get_years and bcr_sub again to re-generate working_routes.csv and working_routes_10.csv and cross-check with legacy copies.

This seems silly to put in rwar...
